{
  "name": "the-marianq-family-herald",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"hugo server -D --bind 0.0.0.0\"",
    "dev:hugo": "hugo server -D --bind 0.0.0.0",
    "dev:tina": "tinacms dev",
    "build": "tinacms build && hugo --gc --minify",
    "build:tina": "tinacms build",
    "build:hugo": "hugo --gc --minify",
    "start": "hugo server",
    "new:post": "hugo new posts/new-post.md",
    "clean": "rm -rf public admin .tina/__generated__"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tinacms/cli": "^1.10.1",
    "@types/node": "^24.0.14",
    "typescript": "^5.0.0",
    "tinacms": "^2.8.1"
  },
  "dependencies": {}
}